<services>{% for service in services %}
    <service>
        <service_code>{{service.id}}</service_code>
        <service_name>{{service.name}}</service_name>
        <description>{{service.hint}}</description>
        <metadata>false</metadata>
        <type>realtime</type>
        <keywords></keywords>
        <group>{{service.category_class.name}}</group>
    </service>{% endfor %}
</services>
    